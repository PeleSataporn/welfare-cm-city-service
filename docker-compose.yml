version: '3'
services:
  db:
    container_name: db
    image: mariadb:10.6.5
    ports:
      - '3306:3306'
    environment:
      - MARIADB_DATABASE=icoop_system
      - MARIADB_USER=root
      - MARIADB_PASSWORD=icoop.cmcity.db@2023$
      - MARIADB_ROOT_PASSWORD=icoop.cmcity.db@2023$
      - MARIADB_ROOT_HOST=%
    volumes:
      - ./db-data:/var/lib/mysql
      - ./init-db:/docker-entrypoint-initdb.d

  app:
    build: .
    ports:
      - '8787:8787'
    depends_on:
      - db

volumes:
  db: